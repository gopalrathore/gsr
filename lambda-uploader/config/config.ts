import { HEADERS } from "./configuration/header";
import { S3_CONFIG, BUCKET_PATH } from "./configuration/s3.configuration";
import { ALLOWED_FILE_TYPE, ALLOWED_FILE_SIZE } from "./configuration/upload-file-validation.conf";

export {
  HEADERS,
  S3_CONFIG,
  BUCKET_PATH,
  ALLOWED_FILE_TYPE,
  ALLOWED_FILE_SIZE
}